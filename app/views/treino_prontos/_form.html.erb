<%= form_with(model: treinos_pronto) do |form| %>
  <% if treinos_pronto.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(treinos_pronto.errors.count, "error") %> prohibited this treinos_pronto from being saved:</h2>

      <ul>
        <% treinos_pronto.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
